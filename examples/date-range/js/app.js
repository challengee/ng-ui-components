(function (window, angular) {
    'use strict';

    angular
        .module('app', ['ngUiComponents'])
        .controller('IndexController', ['$scope', function($scope){

        }]);

})(window, window.angular);